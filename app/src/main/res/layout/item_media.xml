<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginStart="8dp"
    android:layout_marginEnd="8dp">

    <ImageView
        android:id="@+id/mediaPoster"
        android:layout_width="150dp"
        android:layout_height="220dp"
        android:scaleType="fitXY"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/mediaTitle" />

    <TextView
        android:id="@+id/mediaTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="2"
        tools:text="Title"
        android:textSize="16sp"
        android:textAlignment="center"
        android:textColor="#B3A7A7"
        android:layout_marginTop="8dp"
        app:layout_constraintEnd_toEndOf="@id/mediaPoster"
        app:layout_constraintStart_toStartOf="@id/mediaPoster"
        app:layout_constraintTop_toBottomOf="@id/mediaPoster"
        app:flow_verticalBias="0.0"/>

</androidx.constraintlayout.widget.ConstraintLayout>